In order to use this code you need to:
 - Have the batch file and the .jar in the same Directory
 - Run the batch file as an admin
 - Hava Java Installed, duh
 
 That's it! It's pretty easy to use, so it should run pretty simply.